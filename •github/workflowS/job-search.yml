name: Electrical Job Search

on:
  workflow_dispatch:
  schedule:
    - cron: "0 11-23 * * *"

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Confirm workflow works
        run: echo "Electrical Job Search workflow is running."
